/** Layout tokens for the chats split view. */
export const CHATS_SHELL_RADIUS_PX = 14;

/** Match sidebar / `MuiListItemButton` — soft rectangles, not pills. */
export const CHATS_MENU_RADIUS_PX = 12;
